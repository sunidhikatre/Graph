Detect shortest path in directed graph helps in neg cycle

class Solution {
    static int[] bellmanFord(int V, int[][] edges, int src) {
        // Write your code here
        int[] dist = new int[V];
        for(int i = 0; i < V; i++) {
            dist[i] = 100000000;
        }
        dist[src] = 0;
        
        for(int i = 0; i < V; i++){
            for(int[] edge : edges) {
                int u = edge[0];
                int v = edge[1];
                int w = edge[2];
                
                if(dist[u] != 100000000 && dist[u] + w < dist[v])
                {
                    if(i == V-1) {
                        return new int[]{-1};
                    }
                    dist[v] = dist[u] + w;
                }
            }
        }
        return dist;
        
    }
}
